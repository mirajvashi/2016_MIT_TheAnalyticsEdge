airlines <- "AirlinesCluster.csv"
airlines <- read.csv("AirlinesCluster.csv")
head(airlines)
str(airlines)
summary(airline)
summary(airlines)
library(caret)
prep = preProcess(airlines)
class(prep)
str(prep)
summary(prep)
airlinesNorm = predict(prep, airlines)
summary(airlinesNorm)
sd(airlinesNorm$Balance)
sd(airlinesNorm$QualMiles)
barplot(airlinesNorm$QualMiles)
barplot(airlinesNorm$DaysSinceEnroll)
distance = dist(airlinesNorm, method = "euclidian")
hCluster = hclust(distance, method = "ward.D")
plot(hCluster)
rect.hclust(hCluster, K=2)
rect.hclust(hCluster, k=2)
rect.hclust(hCluster, k=3)
plot(hCluster)
rect.hclust(hCluster, k=3)
plot(hCluster)
rect.hclust(hCluster, k=4)
plot(hCluster)
rect.hclust(hCluster, k=6)
plot(hCluster)
rect.hclust(hCluster, k=7)
hClustGroup = cutree(hCluster, k=5)
table(hClustGroup)
tapply(airlines$Balance, hClustGroup, mean)
tapply(airlines$QualMiles, hClustGroup, mean)
tapply(airlines$BonusMiles, hClustGroup, mean)
tapply(airlines$BonusTrans, hClustGroup, mean)
tapply(airlines$FlightMiles, hClustGroup, mean)
tapply(airlines$FlightTrans, hClustGroup, mean)
tapply(airlines$DaysSinceEnroll, hClustGroup, mean)
lapply(split(airlines, clusterGroups), colMeans)
lapply(split(airlines, hClustGroup), colMeans)
round(lapply(split(airlines, hClustGroup), colMeans), 3)
set.seed(88)
kCluster = kmeans(airlinesNorm, centers = 5, iter.max = 1000)
str(kCluster$size)
lapply(split(airlines, hCluster), colMeans)
lapply(split(airlines, kCluster$cluster), colMeans)
lapply(split(airlines, kCluster$cluster), colMeans)
lapply(split(airlines, hClustGroup), colMeans)
